language: python
python:
  - "3.6"
sudo: required
dist: trusty
install:
  - pip3 install numpy
  - pip3 install feedparser

script:
  - python3 main.py
  - bash index.sh

# deploy
deploy:
  - provider: pages
    skip-cleanup: true
    github-token: $GH_TOKEN
    keep-history: false
    on:
      branch: master

